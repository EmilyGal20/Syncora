import { Button, Card, CardContent, Dialog, DialogActions, DialogContent, DialogTitle, MenuItem, TextField, Typography } from '@mui/material'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';import { useState } from 'react';import { useTranslation } from 'react-i18next'
import { api } from '../api/client';import { useAuth } from '../auth/AuthProvider';import { CreateAction } from '../components/CreateAction';import { PageHeader } from '../components/PageHeader';import { EmptyState, ErrorState, LoadingState } from '../components/States'
interface Announcement{id:string;title:string;body:string;priority:string;created_at:string}
export function AnnouncementsPage(){const {t}=useTranslation();const {can}=useAuth();const qc=useQueryClient();const [open,setOpen]=useState(false);const [form,setForm]=useState({title:'',body:'',audience:'organization',priority:'normal'});const q=useQuery({queryKey:['announcements'],queryFn:async()=>(await api.get<Announcement[]>('/announcements')).data});const create=useMutation({mutationFn:()=>api.post('/announcements',{...form,target_user_ids:[],published:true}),onSuccess:()=>{setOpen(false);void qc.invalidateQueries({queryKey:['announcements']})}});return <><PageHeader title={t('announcements.title')} action={can('announcements.manage')?<CreateAction label={t('announcements.new')} onClick={()=>setOpen(true)}/>:undefined}/>{q.isLoading?<LoadingState/>:q.isError?<ErrorState/>:!q.data?.length?<EmptyState title="No announcements" description="Updates for you will appear here."/>:q.data.map(a=><Card key={a.id} sx={{mb:1.5}}><CardContent><Typography variant="h6">{a.title}</Typography><Typography color="text.secondary">{a.body}</Typography></CardContent></Card>)}<Dialog open={open} onClose={()=>setOpen(false)} fullWidth><DialogTitle>{t('announcements.new')}</DialogTitle><DialogContent sx={{display:'grid',gap:2,pt:'12px!important'}}><TextField label="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><TextField label="Content" multiline minRows={4} value={form.body} onChange={e=>setForm({...form,body:e.target.value})}/><TextField select label="Audience" value={form.audience} onChange={e=>setForm({...form,audience:e.target.value})}>{['organization','department','team','users'].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>{t('common.cancel')}</Button><Button variant="contained" disabled={!form.title||!form.body} onClick={()=>create.mutate()}>{t('common.save')}</Button></DialogActions></Dialog></>}

