import ViewKanbanOutlined from '@mui/icons-material/ViewKanbanOutlined';import ViewListOutlined from '@mui/icons-material/ViewListOutlined'
import { Box, Button, Card, CardContent, Chip, Dialog, DialogActions, DialogContent, DialogTitle, MenuItem, TextField, ToggleButton, ToggleButtonGroup, Typography } from '@mui/material'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';import { useState } from 'react';import { useTranslation } from 'react-i18next'
import { api } from '../api/client';import { useAuth } from '../auth/AuthProvider';import { CreateAction } from '../components/CreateAction';import { PageHeader } from '../components/PageHeader';import { EmptyState, ErrorState, LoadingState } from '../components/States';import type { Task } from '../types'
export function TasksPage(){const {t}=useTranslation();const {can}=useAuth();const qc=useQueryClient();const [view,setView]=useState('list');const [open,setOpen]=useState(false);const [form,setForm]=useState({title:'',description:'',priority:'medium',status:'todo',due_date:'',visibility:'private'});const q=useQuery({queryKey:['tasks'],queryFn:async()=>(await api.get<Task[]>('/tasks')).data});const create=useMutation({mutationFn:()=>api.post('/tasks',{...form,due_date:form.due_date||null,tags:[]}),onSuccess:()=>{setOpen(false);setForm({...form,title:'',description:''});void qc.invalidateQueries({queryKey:['tasks']})}});return <><PageHeader title={t('tasks.title')} subtitle={t('tasks.subtitle')} action={can('tasks.create')?<CreateAction label={t('tasks.new')} onClick={()=>setOpen(true)}/>:undefined}/><ToggleButtonGroup exclusive size="small" value={view} onChange={(_,v)=>v&&setView(v)} sx={{mb:2}}><ToggleButton value="list" aria-label="List view"><ViewListOutlined/></ToggleButton><ToggleButton value="board" aria-label="Board view"><ViewKanbanOutlined/></ToggleButton></ToggleButtonGroup>{q.isLoading?<LoadingState/>:q.isError?<ErrorState/>:!q.data?.length?<EmptyState title="No tasks yet" description="Create your first task to start organizing work."/>:<Box sx={{display:'grid',gridTemplateColumns:view==='board'?{xs:'1fr',md:'repeat(4,1fr)'}:'1fr',gap:2}}>{q.data.map(task=><Card key={task.id}><CardContent sx={{display:'flex',gap:2,alignItems:{xs:'start',sm:'center'},flexDirection:{xs:'column',sm:'row'}}}><Box sx={{flex:1}}><Typography fontWeight={650}>{task.title}</Typography><Typography variant="body2" color="text.secondary">{task.description||'No description'}</Typography></Box><Chip size="small" label={task.status.replace('_',' ')} variant="outlined"/><Chip size="small" label={task.priority}/></CardContent></Card>)}</Box>}<Dialog open={open} onClose={()=>setOpen(false)} fullWidth><DialogTitle>{t('tasks.new')}</DialogTitle><DialogContent sx={{display:'grid',gap:2,pt:'12px!important'}}><TextField label="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><TextField label="Description" multiline minRows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><TextField select label="Priority" value={form.priority} onChange={e=>setForm({...form,priority:e.target.value})}>{['low','medium','high','urgent'].map(x=><MenuItem key={x} value={x}>{x}</MenuItem>)}</TextField><TextField label="Due date" type="datetime-local" slotProps={{inputLabel:{shrink:true}}} value={form.due_date} onChange={e=>setForm({...form,due_date:e.target.value})}/></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>{t('common.cancel')}</Button><Button variant="contained" disabled={form.title.length<2||create.isPending} onClick={()=>create.mutate()}>{t('common.save')}</Button></DialogActions></Dialog></>}
