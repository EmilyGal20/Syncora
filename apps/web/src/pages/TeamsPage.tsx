import { Button, Card, CardContent, Dialog, DialogActions, DialogContent, DialogTitle, TextField, Typography } from '@mui/material'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { api } from '../api/client';import { useAuth } from '../auth/AuthProvider';import { CreateAction } from '../components/CreateAction';import { PageHeader } from '../components/PageHeader';import { EmptyState, ErrorState, LoadingState } from '../components/States'
interface Team{id:string;name:string;description:string;is_active:boolean}
export function TeamsPage(){const {t}=useTranslation();const {can}=useAuth();const qc=useQueryClient();const [open,setOpen]=useState(false);const [name,setName]=useState('');const [description,setDescription]=useState('');const q=useQuery({queryKey:['teams'],queryFn:async()=>(await api.get<Team[]>('/teams')).data});const create=useMutation({mutationFn:()=>api.post('/teams',{name,description,member_ids:[],is_active:true}),onSuccess:()=>{setOpen(false);setName('');void qc.invalidateQueries({queryKey:['teams']})}});return <><PageHeader title={t('teams.title')} action={can('teams.manage')?<CreateAction label={t('teams.new')} onClick={()=>setOpen(true)}/>:undefined}/>{q.isLoading?<LoadingState/>:q.isError?<ErrorState/>:!q.data?.length?<EmptyState title="No teams" description="Teams you can access will appear here."/>:q.data.map(team=><Card key={team.id} sx={{mb:1.5}}><CardContent><Typography variant="h6">{team.name}</Typography><Typography color="text.secondary">{team.description}</Typography></CardContent></Card>)}<Dialog open={open} onClose={()=>setOpen(false)} fullWidth><DialogTitle>{t('teams.new')}</DialogTitle><DialogContent sx={{display:'grid',gap:2,pt:'12px!important'}}><TextField label="Name" value={name} onChange={e=>setName(e.target.value)}/><TextField label="Description" multiline value={description} onChange={e=>setDescription(e.target.value)}/></DialogContent><DialogActions><Button onClick={()=>setOpen(false)}>{t('common.cancel')}</Button><Button variant="contained" disabled={name.length<2} onClick={()=>create.mutate()}>{t('common.save')}</Button></DialogActions></Dialog></>}

