export interface User { id:string; username:string; email:string|null; full_name:string; first_name?:string; last_name?:string; phone?:string|null; job_title?:string|null; notes?:string|null; must_change_password?:boolean; is_active:boolean; organization_id:string; department_id:string|null; team_id:string|null; locale:string|null; roles:string[]; permissions:string[]; scopes:Record<string,'OWN'|'TEAM'|'DEPARTMENT'|'ORGANIZATION'>; last_login_at:string|null; created_at:string;is_platform_admin:boolean;workspace_name:string;workspace_slug:string;workspace_type:'general'|'band';workspace_logo_url:string|null;workspace_primary_color:string;workspace_secondary_color:string;enabled_modules:string[] }
export interface NavItem { id:string; parent_id:string|null; title:string; icon:string; route:string|null; item_type:string; order:number; required_permission:string|null; module:string|null; enabled:boolean }
export interface Widget { id:string; type:string; title:string; position:number; width:number; height:number; configuration:Record<string, unknown> }
export interface Dashboard { id:string; name:string; widgets:Widget[] }
export interface Task { id:string; title:string; description:string; status:'todo'|'in_progress'|'blocked'|'completed'; priority:string; due_date:string|null; tags:string[] }
